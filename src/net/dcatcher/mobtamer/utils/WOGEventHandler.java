package net.dcatcher.mobtamer.utils;

public class WOGEventHandler {

	
/*	@ForgeSubscribe
	public void onInteract(EntityInteractEvent event){
		EntityPlayer player = event.entityPlayer;
		if(event.entityLiving != null && event.entityLiving.isChild()){
			if(event.entityPlayer.getCurrentEquippedItem() != null && event.entityPlayer.getCurrentEquippedItem().itemID == ItemHandler.wandOfGrowth.itemID)
				((EntityAgeable)event.entityLiving).setGrowingAge(0);
		}
	
	}
	*/
}
